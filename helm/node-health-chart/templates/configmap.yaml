apiVersion: v1
kind: ConfigMap
metadata:
  name: node-health-configmap
  namespace: {{ .Values.namespace }}
  labels:
    app: node-health
data:
  config.yaml: |
    server:
      listen:
        address: 'http://0.0.0.0:8000'
    service:
      kubernetes:
        address: ''
        inCluster: true
        tls:
          caFile: ''
          crtFile: ''
          keyFile: ''
